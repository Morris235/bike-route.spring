type Course {
    id: String!
    name: String!
    rate: Int
    finish_time: String
    owner_id: String
    created: String
    deleted: Boolean
}

input CourseInput {
    id: String!
    name: String!
    rate: Int
    finish_time: String
    owner_id: String
    created: String
    deleted: Boolean
}

type Mutation {
    createCourse(id: String!, name: String!, rate: Int, finish_time: String, owner_id: String): Course!
    updateCourse(id: String!, name: String!, rate: Int, finish_time: String, owner_id: String): Course
    deleteCourseById(id: String!): Boolean
}